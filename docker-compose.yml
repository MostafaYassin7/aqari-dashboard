services:
  admin:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_URL: https://api.aqora.sa/api/v1
    container_name: aqar_admin
    restart: always
    ports:
      - "5000:5000"